<div class="container small-container">
<h2 class="center form-title">管理者　ログイン</h2>

<%= form_with(scope: :session, url: admin_sessions_path, method: :post, local: true) do |f| %>
  <%= render "form", f: f %>
<% end %>

<p class="form-input">新規登録は<%= link_to "こちら", new_admin_registration_path %></p>

</div>
